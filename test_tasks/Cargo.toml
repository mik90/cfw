[package]
name = "test_tasks"
version = "0.1.0"
edition = "2024"
# This is just some tasks for testing
publish = false

[dependencies]
task = { path = "../task" }
task_macros = { path = "../task_macros" }

[lints]
workspace = true

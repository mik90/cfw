[package]
name = "simulation_executor"
version = "0.1.0"
edition = "2024"

[dependencies]
task = { path = "../task" }

[dev-dependencies]
test_tasks = { path = "../test_tasks" }

[lints]
workspace = true

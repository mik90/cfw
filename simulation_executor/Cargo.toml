[package]
name = "simulation_executor"
version = "0.1.0"
edition = "2024"

[dependencies]
task = { path = "../task" }
rayon = "1"

[dev-dependencies]
task = { path = "../task", features = ["test-utils"] }

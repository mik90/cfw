
[workspace]
members = [
    "exact_replay_executor",
    "simulation_executor",
    "task",
    "task_builder",
    "task_macros",
    "live_executor",
    "live_replay_executor",
    "testing",
]
resolver = "3"

[workspace.lints.clippy]
all = "deny"
undocumented_unsafe_blocks = "deny"


[workspace.lints.rust]
dead_code = "allow" # Not ideal, but im iterating a lot and this is getting chatty

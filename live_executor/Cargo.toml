[package]
name = "live_executor"
version = "0.1.0"
edition = "2024"

[lib]

[dependencies]
task = { path = "../task" }
crossbeam = "0.8"

[dev-dependencies]
test_tasks = { path = "../test_tasks" }
assert_no_alloc = { version = "1", default-features = false, features = ["warn_debug"] }

[lints]
workspace = true

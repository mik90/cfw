[package]
name = "live_replay_executor"
version = "0.1.0"
edition = "2024"

[dependencies]
task = { path = "../task" }
logging = { path = "../logging" }
live_executor = { path = "../live_executor" }

[lints]
workspace = true
